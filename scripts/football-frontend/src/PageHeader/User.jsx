// src/components/PageHeader/User.jsx
import React from "react";
// styling
import styles from './styles.module.scss';

const User = () => {
  return (
    <div className="d-flex align-items-center g-16">
      {/* Empty user section — no avatar, no name */}
    </div>
  );
};

export default User;
